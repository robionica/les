
LES -- Local Elimination Solver

Getting Started
===============

Add path to LES library for your projects:

  $ LD_LIBRARY_PATH=/somewhere/les/lib/

or add "-Wl,--rpath -Wl,/somewhere/les/lib".

Known issues
============

Fix "/usr/lib/i386-linux-gnu/libstdc++.so.6: version `GLIBCXX_3.4.15' not found ..." as follows:

  $ export LD_LIBRARY_PATH=/usr/local/lib:/usr/lib
